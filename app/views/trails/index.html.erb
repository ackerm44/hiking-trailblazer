<h1>All Trails Trekked</h1>
<hr>
<br>
<div>
  <% Region.all.each do |region| %>
    <h2><%= region.name %></h2>
    <% region.trails.each do |trail| %>
    <ul>
      <li><a href="/trails/<%= trail.slug %>"><%= trail.name %> | <%= trail.length %> miles</a></li>
    </ul>
    <% end %>
  <% end %>
</div>
